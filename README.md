# Question & Answer API

## Описание проекта

Тестовое задание для позиции Python-разработчика. REST API сервис для вопросов и ответов, разработанный с использованием FastAPI и SQLite/PostgreSQL.

## Цели проекта

### Демонстрация навыков
- **Разработка через тестирование (TDD)** - весь функционал реализован через цикл "красный-зеленый-рефакторинг"
- **Чистая архитектура** - разделение на слои: API, схемы, модели
- **Современный стек** - FastAPI, SQLAlchemy 2.0, Pydantic v2
- **Профессиональные практики** - типизация, валидация, обработка ошибок

### Соответствие требованиям
- Модульная структура проекта
- Полный CRUD для вопросов и ответов
- Каскадное удаление связанных данных
- Валидация входных данных
- Тестовое покрытие

## Функциональность

### ✅ Реализовано
**Вопросы (Questions):**
- `GET /questions/` - список всех вопросов (с пагинацией)
- `POST /questions/` - создать новый вопрос
- `GET /questions/{id}` - получить вопрос
- `DELETE /questions/{id}` - удалить вопрос (с каскадным удалением ответов)

**Ответы (Answers):**
- `POST /questions/{id}/answers/` - добавить ответ к вопросу
- `GET /questions/{question_id}/answers/{answer_id}/` - получить конкретный ответ
- `DELETE /questions/{question_id}/answers/{answer_id}/` - удалить ответ
- `GET /questions/{question_id}/answers/` - список ответов на вопрос

### 🔄 В разработке
- `GET /questions/{id}` - возврат вопроса со всеми ответами
- Интеграция с PostgreSQL
- Docker-контейнеризация
- Миграции базы данных (Alembic)

## Технический стек

- **FastAPI** - веб-фреймворк
- **SQLAlchemy 2.0** - ORM (современный синтаксис)
- **SQLite** - база данных (разработка)
- **PostgreSQL** - база данных (production)
- **Pydantic v2** - валидация данных с ConfigDict
- **pytest** - тестирование

## Архитектурные решения

### Многослойная архитектура
- **API слой** - обработка HTTP запросов, маршрутизация
- **Схемы (Schemas)** - валидация входящих/исходящих данных (Pydantic)
- **Модели (Models)** - описание структуры базы данных (SQLAlchemy)

### TDD подход
Каждая функция реализована через строгий цикл:
1. **RED** - написание падающего теста
2. **GREEN** - минимальная реализация для прохождения теста  
3. **REFACTOR** - улучшение кода при сохранении зеленых тестов

### Ключевые особенности
- **Type hints** - полная типизация всего кода
- **SQLAlchemy 2.0** - современный синтаксис с `select()`, `scalars()`
- **Обработка ошибок** - корректные HTTP статусы и сообщения
- **Валидация** - проверка данных на всех уровнях
- **Каскадное удаление** - автоматическое удаление ответов при удалении вопроса

## Модели данных

### Question (Вопрос)
```python
id: int (primary key)
text: str (1000 chars)
created_at: datetime
answers: Relationship[Answer]  # каскадное удаление
```

### Answer (Ответ)
```python
id: int (primary key)
text: str (1000 chars)
user_id: str (255 chars)  # идентификатор пользователя
question_id: int (foreign key)
created_at: datetime
```

## Запуск проекта

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск тестов
pytest

# Запуск приложения
uvicorn src.main:app --reload
```

## Тестирование

```bash
# Все тесты
pytest

# Конкретный тест
pytest tests/test_questions.py -v
pytest tests/test_answers.py -v

# С отладочным выводом
pytest -v -s
```

## Документация API

После запуска приложения документация доступна по адресу:
- `http://localhost:8000/docs` - интерактивная документация Swagger
- `http://localhost:8000/redoc` - альтернативная документация ReDoc

## Статус разработки

**✅ Базовый функционал реализован** - работают все основные CRUD операции для вопросов и ответов согласно ТЗ.

**🔧 В процессе** - улучшение эндпоинтов, добавление возврата вопросов с ответами, подготовка к Docker-развертыванию.

**📋 Планируется** - интеграция с PostgreSQL, настройка миграций, контейнеризация.
